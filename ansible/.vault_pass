#!/usr/bin/python3
import os
import sys


try:
    password = os.environ['ANSIBLE_VAULT_PASS']
except KeyError:
    print(f"Please provide value for environmental variable: ANSIBLE_VAULT_PASS", file=sys.stderr)
    sys.exit(0)
print(password)
